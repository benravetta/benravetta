{{#if @labs.members}}
  {{#unless @member}}
    <div class='c-widget'>
      <div class='c-subscribe'>
        <h3 class='u-type-500 u-mb-8'>{{t 'Love the Lakes?' siteTitle=@site.title}}</h3>
        <p class='u-type-500 u-mb-16'>{{t 'Subscribe to the #onlyinthelakes newsletter, to receive exceedingly good content to your inbox.' }}</p>
        {{> subscribe-form inputLabel='subscribe-email-sidebar'}}
      </div>
    </div>
  {{/unless}}
{{/if}}