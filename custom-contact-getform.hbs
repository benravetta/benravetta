<!DOCTYPE html>
<html lang="{{@site.lang}}">
	<head>
		<meta charset="utf-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<title>{{@site.title}} — {{meta_title}}</title>
		<meta name="HandheldFriendly" content="True">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		{{>services/google_fonts}}
		<link rel="stylesheet" type="text/css" href="{{asset "css/screen.css"}}">
		{{ghost_head}}
	</head>
	<body class="custom-page{{#get "tags" limit="all" filter="visibility:internal"}}{{#foreach tags visibility="internal"}} global-{{slug}}{{/foreach}}{{/get}}">
		{{#post}}
		<div class="custom-wrap">
			{{#if feature_image}}
			<div class="custom-image global-bg-image" style="background-image: url({{feature_image}})"></div>
			{{/if}}
			<div class="custom-container">
				<div class="custom-logo">
					{{#if @site.logo}}
					<a class="is-image" href="{{@site.url}}"><img src="{{@site.logo}}" alt="{{@site.title}}"></a>
					{{else}}
					<a class="is-title" href="{{@site.url}}">{{@site.title}}</a>
					{{/if}}
				</div>
				<div class="custom-content">
					<form method="POST" action={{>services/getform}} enctype="multipart/form-data">
						<h1>Contact us or submit image(s).<a href="#">See terms for content submission.</a></h1>
						<label for="name">{{t "Name"}}</label>
						<input id="name" type="text" name="name" required>
						<label for="email">{{t "Email address"}}</label>
						<input id="email" type="email" name="email" required>
						<label for="message">{{t "Message"}}</label>
						<textarea id="message" name="message" required></textarea>
						<label for="file">{{t "Submit image (5MB max size)"}}</label>
						<input type="file" name="file">
						<button class="global-button" type="submit">{{t "Send message"}}</button>
					</form>
				</div>
			</div>
		</div>
		{{/post}}
		{{ghost_foot}}
	</body>
</html>